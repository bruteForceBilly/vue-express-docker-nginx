version: "3.7"
services:
    api:
        build:
            context: ./server
        volumes:
            - /app/node_modules
            - ./server:/app
    client:
        build:
            context: ./client
        volumes:
            - /app/node_modules
            - ./client:/app

        