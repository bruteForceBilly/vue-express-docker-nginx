version: '3'
services:
    client:
        build:
            context: ./client
        ports:
            - 80:80
    api:
        build:
            context: ./api
        ports:
            - 5000:5000